/// Which feed to request — maps to the API list endpoints:
/// `all` → `/v1/wallpapers`, `live` → `/v1/wallpapers/live`,
/// `still` → `/v1/wallpapers/static`.
enum WallpaperType { all, live, still }
